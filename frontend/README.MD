# UrbanPulse frontend

## Local frontend env

To run in dev mode, create a `.env` file:

```txt
# Optional: keep empty when using same-origin /api proxy.
VITE_API_BASE_URL=
VITE_MAPBOX_TOKEN=pk...
```